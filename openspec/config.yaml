schema: spec-driven-review

# Project context (optional)
# This is shown to AI when creating artifacts.
context: |
  openspec/discovery.md is the release plan. When asked to explore the next change without 
  a specific request, pick the next unchecked story in discovery.md and use its story packet 
  as the input. One story per change.
